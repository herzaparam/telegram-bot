---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/config.py
  - src/logging.py
  - src/db/__init__.py
  - src/db/models.py
  - src/db/database.py
  - src/pipeline/__init__.py
  - src/bot/__init__.py
  - docker-compose.yml
  - .env.example
  - alembic.ini
  - src/db/migrations/env.py
  - src/db/migrations/script.py.mako
  - src/db/migrations/versions/001_initial_schema.py
  - .pre-commit-config.yaml
  - .ruff.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_db/__init__.py
  - tests/test_db/test_models.py
autonomous: true
requirements:
  - DATA-05
must_haves:
  truths:
    - "Settings load from .env with validation and sensible defaults"
    - "SQLAlchemy models define assets, pipeline_runs, pipeline_asset_runs, and daily_decisions tables"
    - "daily_decisions has decision_price, decision_price_at, evaluation_price, evaluation_price_at columns"
    - "Alembic migration creates all Phase 1 tables in TimescaleDB"
    - "docker compose up starts TimescaleDB and passes health check"
    - "Seed data populates assets table with BBCA.JK, BTC/USDT, ETH/USDT and others"
  artifacts:
    - path: "src/config.py"
      provides: "Centralized pydantic-settings configuration"
      contains: "class Settings(BaseSettings)"
    - path: "src/db/models.py"
      provides: "ORM models for Phase 1 tables"
      exports: ["Base", "Asset", "PipelineRun", "PipelineAssetRun", "DailyDecision"]
    - path: "src/db/database.py"
      provides: "Async engine and session factory"
      contains: "async_sessionmaker"
    - path: "docker-compose.yml"
      provides: "Dev TimescaleDB container"
      contains: "timescale/timescaledb:2.18.0-pg16"
    - path: "alembic.ini"
      provides: "Alembic configuration"
      contains: "sqlalchemy.url"
  key_links:
    - from: "src/db/models.py"
      to: "src/config.py"
      via: "Base metadata with naming conventions"
      pattern: "naming_convention"
    - from: "src/db/database.py"
      to: "src/config.py"
      via: "settings.database_url"
      pattern: "settings\\.database_url"
    - from: "src/db/migrations/env.py"
      to: "src/db/models.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata"
---

<objective>
Set up the complete project infrastructure: dependencies, configuration, Docker Compose for TimescaleDB, SQLAlchemy models for all Phase 1 tables, Alembic migrations, structured logging, dev tooling (ruff, mypy, pre-commit), and test scaffolding.

Purpose: Every subsequent plan depends on config, models, database connectivity, and dev tooling being in place. This is the foundation everything builds on.
Output: Working dev environment with `docker compose up` providing TimescaleDB, all Phase 1 tables migrated, seed data loaded, and tests passing.
</objective>

<execution_context>
@/Users/herza/rapier/trade-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herza/rapier/trade-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@plan/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project setup, config, Docker, and database models</name>
  <files>
    pyproject.toml, src/__init__.py, src/config.py, src/logging.py,
    src/db/__init__.py, src/db/models.py, src/db/database.py,
    src/pipeline/__init__.py, src/bot/__init__.py,
    docker-compose.yml, .env.example, .ruff.toml, .pre-commit-config.yaml,
    tests/__init__.py, tests/conftest.py, tests/test_config.py,
    tests/test_db/__init__.py, tests/test_db/test_models.py
  </files>
  <read_first>
    pyproject.toml
    plan/ARCHITECTURE.md
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Settings loads DATABASE_URL from env with default "postgresql+asyncpg://trade:trade@localhost:5432/trade_agent"
    - Test: Settings validates OPENAI_API_KEY as SecretStr
    - Test: Settings reads LOG_LEVEL with default "INFO"
    - Test: Asset model has columns: id, symbol, name, asset_type, exchange, yfinance_symbol, ccxt_symbol, is_active, created_at
    - Test: Asset model has unique constraint on (symbol, asset_type)
    - Test: PipelineRun model has columns: id (UUID), run_date, stage, status, started_at, completed_at, metadata_
    - Test: PipelineRun has unique constraint on (run_date, stage)
    - Test: PipelineAssetRun model has columns: id, run_id (FK pipeline_runs.id), asset_id (FK assets.id), status, started_at, completed_at, error_message, retry_count
    - Test: PipelineAssetRun has unique constraint on (run_id, asset_id)
    - Test: DailyDecision model has columns: decision_price, decision_price_at (timestamptz), evaluation_price (nullable), evaluation_price_at (nullable timestamptz)
    - Test: DailyDecision has unique constraint on (asset_id, date)
    - Test: Base.metadata has naming_convention with keys ix, uq, ck, fk, pk
  </behavior>
  <action>
    **1. Install dependencies via uv:**
    ```bash
    uv add pydantic-settings sqlalchemy[asyncio] asyncpg alembic litellm structlog tenacity httpx
    uv add --dev pytest pytest-asyncio ruff mypy pre-commit
    ```

    **2. Add pytest config to pyproject.toml:**
    ```toml
    [tool.pytest.ini_options]
    asyncio_mode = "auto"
    testpaths = ["tests"]
    pythonpath = ["."]

    [tool.mypy]
    python_version = "3.13"
    strict = true
    plugins = ["pydantic.mypy"]

    [tool.ruff]
    target-version = "py313"
    line-length = 120
    ```

    **3. Create src/config.py** with pydantic-settings BaseSettings:
    - `database_url: str` default `"postgresql+asyncpg://trade:trade@localhost:5432/trade_agent"`
    - `database_url_sync: str` default `"postgresql://trade:trade@localhost:5432/trade_agent"` (for Alembic)
    - `openai_api_key: SecretStr` default `SecretStr("")`
    - `llm_primary_model: str` default `"gpt-4o-mini"`
    - `llm_fallback_model: str` default `"gemini/gemini-2.0-flash"`
    - `llm_max_retries: int` default `3`
    - `llm_timeout: int` default `30`
    - `telegram_bot_token: SecretStr` default `SecretStr("")`
    - `telegram_chat_id: str` default `""`
    - `log_level: str` default `"INFO"`
    - `log_format: str` default `"json"`
    - `timeout_fetch: int` default `60`
    - `timeout_analyze: int` default `120`
    - `timeout_llm: int` default `30`
    - SettingsConfigDict: `env_file=".env"`, `env_file_encoding="utf-8"`, `case_sensitive=False`
    - Module-level `settings = Settings()` singleton

    **4. Create src/logging.py** with `setup_logging(log_level, log_format)`:
    - structlog configuration with contextvars, log level, logger name, ISO timestamps, stack info
    - JSON renderer for production (`log_format="json"`), ConsoleRenderer for dev (`log_format="console"`)
    - Configure stdlib logging root handler with structlog formatter
    - Follow exact pattern from RESEARCH.md Pattern 5

    **5. Create src/db/models.py:**
    - Define naming convention dict: `{"ix": "ix_%(column_0_label)s", "uq": "uq_%(table_name)s_%(column_0_name)s", "ck": "ck_%(table_name)s_%(constraint_name)s", "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s", "pk": "pk_%(table_name)s"}`
    - `Base = DeclarativeBase` with `metadata=MetaData(naming_convention=convention)`
    - `Asset` model: id (Integer PK), symbol (String(20) NOT NULL), name (String(100)), asset_type (String(10) NOT NULL), exchange (String(20)), yfinance_symbol (String(20)), ccxt_symbol (String(30)), is_active (Boolean default True), created_at (DateTime(timezone=True) server_default=func.now()). UniqueConstraint("symbol", "asset_type").
    - `PipelineRun` model: id (Uuid PK default uuid4), run_date (Date NOT NULL), stage (String(20) NOT NULL), status (String(20) NOT NULL), started_at (DateTime(tz) nullable), completed_at (DateTime(tz) nullable), metadata_ mapped_column("metadata", JSONB, nullable). UniqueConstraint("run_date", "stage").
    - `PipelineAssetRun` model: id (Integer PK), run_id (Uuid FK pipeline_runs.id NOT NULL), asset_id (Integer FK assets.id NOT NULL), status (String(20) NOT NULL default "pending"), started_at (DateTime(tz) nullable), completed_at (DateTime(tz) nullable), error_message (Text nullable), retry_count (Integer default 0). UniqueConstraint("run_id", "asset_id").
    - `DailyDecision` model: id (Integer PK), asset_id (Integer FK assets.id NOT NULL), date (Date NOT NULL), verdict (String(15) NOT NULL), score (Numeric(4,3) nullable), confidence (Numeric(4,3) nullable), decision_price (Numeric(20,8)), decision_price_at (DateTime(tz)), evaluation_price (Numeric(20,8) nullable), evaluation_price_at (DateTime(tz) nullable), all_signals (JSONB nullable), reasoning (Text nullable), key_factors (JSONB nullable), risk_warning (Text nullable), wait_for (Text nullable), lessons_applied (JSONB nullable), model_used (String(50) nullable), created_at (DateTime(tz) server_default=func.now()). UniqueConstraint("asset_id", "date").
    - Add SEED_ASSETS list with 6 assets: BBCA (stock/IDX/BBCA.JK), BBRI (stock/IDX/BBRI.JK), TLKM (stock/IDX/TLKM.JK), BTC (crypto/binance/BTC/USDT), ETH (crypto/binance/ETH/USDT), SOL (crypto/binance/SOL/USDT).

    **6. Create src/db/database.py:**
    - `engine = create_async_engine(settings.database_url, echo=False, pool_size=5, max_overflow=10)`
    - `async_session_factory = async_sessionmaker(engine, expire_on_commit=False)`
    - `async def get_session()` async generator yielding session
    - `async def init_db()` for creating engine (used by pipeline/bot startup)

    **7. Create docker-compose.yml** (dev - TimescaleDB only):
    - Service `db`: image `timescale/timescaledb:2.18.0-pg16`, ports `5432:5432`
    - env: POSTGRES_DB=trade_agent, POSTGRES_USER=trade, POSTGRES_PASSWORD=trade_dev
    - volume pgdata:/var/lib/postgresql/data, shm_size "64mb"
    - healthcheck: pg_isready -U trade -d trade_agent, interval 10s, timeout 5s, retries 5, start_period 10s

    **8. Create .env.example** with all settings keys and example values (no real secrets).

    **9. Create .ruff.toml:** target-version = "py313", line-length = 120, select = ["E", "F", "I", "W", "UP", "B", "SIM"], src = ["src", "tests"].

    **10. Create .pre-commit-config.yaml:** ruff lint + ruff format hooks using `rev: v0.15.7` from `https://github.com/astral-sh/ruff-pre-commit`.

    **11. Create empty __init__.py** files for: src/, src/db/, src/pipeline/, src/bot/, tests/, tests/test_db/.

    **12. Write tests** in tests/test_config.py (Settings loading) and tests/test_db/test_models.py (model column existence, unique constraints, naming conventions, DailyDecision look-ahead bias columns).

    **13. Create tests/conftest.py** with shared fixtures:
    - `test_settings` fixture returning Settings with test database_url
    - Basic model instantiation fixtures
  </action>
  <verify>
    <automated>cd /Users/herza/rapier/trade-agent && uv run pytest tests/test_config.py tests/test_db/test_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains "pydantic-settings" and "sqlalchemy" and "alembic" and "litellm" and "structlog" in dependencies
    - pyproject.toml contains asyncio_mode = "auto" under [tool.pytest.ini_options]
    - src/config.py contains "class Settings(BaseSettings)" and "database_url" and "openai_api_key: SecretStr" and "settings = Settings()"
    - src/logging.py contains "def setup_logging" and "structlog.configure" and "JSONRenderer" and "ConsoleRenderer"
    - src/db/models.py contains "class Asset" and "class PipelineRun" and "class PipelineAssetRun" and "class DailyDecision"
    - src/db/models.py contains "naming_convention" and "DeclarativeBase"
    - src/db/models.py contains "decision_price_at" and "evaluation_price" and "evaluation_price_at"
    - src/db/models.py contains "SEED_ASSETS" with at least "BBCA" and "BTC" and "ETH"
    - src/db/database.py contains "create_async_engine" and "async_sessionmaker" and "async def get_session"
    - docker-compose.yml contains "timescale/timescaledb:2.18.0-pg16" and "pg_isready"
    - .env.example contains "DATABASE_URL=" and "OPENAI_API_KEY=" and "TELEGRAM_BOT_TOKEN="
    - tests/test_db/test_models.py exits 0
    - tests/test_config.py exits 0
  </acceptance_criteria>
  <done>
    All dependencies installed. Settings class validates and loads from env. All four ORM models defined with correct columns, types, and constraints. Database async engine and session factory created. Docker Compose starts TimescaleDB with health check. Dev tooling configured. Tests pass for config and models.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic migrations with seed data</name>
  <files>
    alembic.ini, src/db/migrations/env.py, src/db/migrations/script.py.mako,
    src/db/migrations/versions/001_initial_schema.py
  </files>
  <read_first>
    src/db/models.py
    src/config.py
    src/db/database.py
    plan/ARCHITECTURE.md
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    **1. Create alembic.ini:**
    - `script_location = src/db/migrations`
    - `sqlalchemy.url` set to `postgresql://trade:trade@localhost:5432/trade_agent` (sync URL for Alembic offline mode; online mode uses async from settings)
    - `file_template = %%(rev)s_%%(slug)s`

    **2. Create src/db/migrations/env.py** (async Alembic):
    - Import `from src.db.models import Base` for `target_metadata = Base.metadata`
    - Import `from src.config import settings` to get `database_url`
    - `run_migrations_offline()`: use `config.get_main_option("sqlalchemy.url")`, configure context with target_metadata and literal_binds=True
    - `run_migrations_online()`: use `asyncio.run(run_async_migrations())`
    - `run_async_migrations()`: create async engine from `settings.database_url`, connect, run_sync(do_run_migrations), dispose
    - `do_run_migrations(connection)`: configure context with connection and target_metadata, begin_transaction, run_migrations
    - Follow exact pattern from RESEARCH.md "Alembic Async env.py"

    **3. Create src/db/migrations/script.py.mako** (standard Alembic template).

    **4. Create initial migration src/db/migrations/versions/001_initial_schema.py:**
    - `op.execute("CREATE EXTENSION IF NOT EXISTS timescaledb")` at the top of upgrade()
    - Create `assets` table with all columns from the Asset model
    - Create `pipeline_runs` table with all columns from PipelineRun model
    - Create `pipeline_asset_runs` table with all columns from PipelineAssetRun model
    - Create `daily_decisions` table with all columns from DailyDecision model including decision_price, decision_price_at, evaluation_price, evaluation_price_at
    - All unique constraints and foreign keys matching model definitions
    - Indexes: `idx_pipeline_runs_date` on pipeline_runs(run_date DESC), `idx_decisions_asset_date` on daily_decisions(asset_id, date DESC)
    - Seed the assets table with SEED_ASSETS data using `op.bulk_insert()`
    - `downgrade()`: drop tables in reverse FK order (daily_decisions, pipeline_asset_runs, pipeline_runs, assets)

    **5. Run the migration against the running TimescaleDB container:**
    ```bash
    docker compose up -d db
    # wait for healthy
    uv run alembic upgrade head
    ```

    **6. Verify tables exist:**
    ```bash
    docker compose exec db psql -U trade -d trade_agent -c "\\dt"
    docker compose exec db psql -U trade -d trade_agent -c "SELECT symbol, asset_type FROM assets"
    ```
  </action>
  <verify>
    <automated>cd /Users/herza/rapier/trade-agent && docker compose up -d db && sleep 5 && uv run alembic upgrade head && docker compose exec db psql -U trade -d trade_agent -c "SELECT count(*) FROM assets" | grep -q "6"</automated>
  </verify>
  <acceptance_criteria>
    - alembic.ini contains "script_location = src/db/migrations"
    - src/db/migrations/env.py contains "from src.db.models import Base" and "target_metadata = Base.metadata" and "async_engine_from_config" or "create_async_engine"
    - src/db/migrations/versions/ contains a file with "CREATE EXTENSION IF NOT EXISTS timescaledb" or "create_extension" and "assets" and "pipeline_runs" and "pipeline_asset_runs" and "daily_decisions"
    - Migration file contains "decision_price_at" and "evaluation_price" and "evaluation_price_at"
    - Migration file contains "bulk_insert" or "INSERT" with "BBCA" and "BTC" and "ETH"
    - `uv run alembic upgrade head` exits 0
    - psql query `SELECT count(*) FROM assets` returns 6
    - psql query `SELECT column_name FROM information_schema.columns WHERE table_name='daily_decisions'` includes decision_price_at and evaluation_price_at
  </acceptance_criteria>
  <done>
    Alembic configured for async. Initial migration creates all Phase 1 tables with TimescaleDB extension enabled. Assets table seeded with 6 default assets. Migration runs successfully against TimescaleDB. All constraints and indexes in place.
  </done>
</task>

</tasks>

<verification>
- `docker compose up -d db` starts TimescaleDB and passes health check
- `uv run alembic upgrade head` creates all tables
- `uv run pytest tests/test_config.py tests/test_db/test_models.py -x -v` passes
- `docker compose exec db psql -U trade -d trade_agent -c "SELECT symbol, asset_type FROM assets"` shows 6 rows
- `uv run ruff check src/` exits clean
</verification>

<success_criteria>
- TimescaleDB running in Docker with health check passing
- All 4 models (Asset, PipelineRun, PipelineAssetRun, DailyDecision) defined and migrated
- daily_decisions has look-ahead bias prevention columns (decision_price_at, evaluation_price, evaluation_price_at)
- Seed assets loaded (BBCA.JK, BBRI.JK, TLKM.JK, BTC/USDT, ETH/USDT, SOL/USDT)
- pydantic-settings config loading and validating
- structlog configured for JSON/console output
- Dev tooling (ruff, mypy, pre-commit) configured
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
