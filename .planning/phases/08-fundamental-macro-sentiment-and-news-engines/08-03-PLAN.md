---
phase: 08-fundamental-macro-sentiment-and-news-engines
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/engines/fundamental.py
  - src/engines/macro.py
  - src/engines/sentiment.py
  - src/engines/event.py
  - src/llm/news_analyzer.py
  - tests/test_engines/test_fundamental.py
  - tests/test_engines/test_macro.py
  - tests/test_engines/test_sentiment.py
  - tests/test_engines/test_event.py
  - tests/test_llm/test_news_analyzer.py
autonomous: true
requirements: [ENGN-02, ENGN-05, ENGN-09, ENGN-12, NEWS-03]

must_haves:
  truths:
    - "FundamentalEngine produces valid score/confidence for IDX stocks using zone-mapped P/E, P/B, ROE, revenue growth, dividend yield, debt/equity"
    - "FundamentalEngine returns score=0/confidence=0 with reasoning 'Fundamentals not applicable for crypto' for crypto assets (per D-03)"
    - "MacroEngine produces a context score from FRED macro indicators and highlights IDX vs crypto relevant factors in reasoning"
    - "SentimentEngine combines Fear & Greed and Reddit sentiment, degrades to Fear & Greed alone when Reddit unavailable"
    - "EventEngine reads news_events table and surfaces upcoming events by category"
    - "LLM news analyzer batch-scores headlines with impact_score, affected_assets, and category per headline"
  artifacts:
    - path: "src/engines/fundamental.py"
      provides: "FundamentalEngine with zone-mapping scoring"
      contains: "class FundamentalEngine"
    - path: "src/engines/macro.py"
      provides: "MacroEngine with FRED indicator scoring"
      contains: "class MacroEngine"
    - path: "src/engines/sentiment.py"
      provides: "SentimentEngine with Fear & Greed + Reddit"
      contains: "class SentimentEngine"
    - path: "src/engines/event.py"
      provides: "EventEngine reading news_events"
      contains: "class EventEngine"
    - path: "src/llm/news_analyzer.py"
      provides: "score_news_impact() batch LLM scoring"
      contains: "async def score_news_impact"
  key_links:
    - from: "src/engines/fundamental.py"
      to: "src/engines/base.py"
      via: "BaseEngine ABC subclass"
      pattern: "class FundamentalEngine\\(BaseEngine\\)"
    - from: "src/llm/news_analyzer.py"
      to: "src/llm/client.py"
      via: "llm_completion with JSON mode"
      pattern: "llm_completion"
---

<objective>
Create four new analysis engines (Fundamental, Macro, Sentiment, Event) following the BaseEngine contract, plus the LLM news impact scorer. Engines use the store-backed pattern: data is injected via constructor, not read from the price DataFrame.

Purpose: These engines expand signal quality from 2 engines (technical + quantitative) to 6, giving the LLM decision maker richer data.
Output: Four engine modules, one LLM news analyzer module, all with comprehensive tests.
</objective>

<execution_context>
@/Users/herza/rapier/trade-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herza/rapier/trade-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fundamental-macro-sentiment-and-news-engines/08-CONTEXT.md
@.planning/phases/08-fundamental-macro-sentiment-and-news-engines/08-RESEARCH.md
@.planning/phases/08-fundamental-macro-sentiment-and-news-engines/08-01-SUMMARY.md

<interfaces>
From src/engines/base.py:
```python
@dataclass(frozen=True)
class Signal:
    category: str
    score: float       # -1.0 to +1.0
    confidence: float  # 0.0 to 1.0
    reasoning: str
    indicators: dict
    data_quality: dict

class BaseEngine(ABC):
    @abstractmethod
    def analyze(self, asset_id: int, asset_symbol: str, df: pd.DataFrame) -> Signal: ...
    @property
    @abstractmethod
    def category(self) -> str: ...
    @property
    def supports_stocks(self) -> bool: return True
    @property
    def supports_crypto(self) -> bool: return True
```

From src/engines/technical.py (pattern to replicate):
```python
class TechnicalEngine(BaseEngine):
    @property
    def category(self) -> str: return "technical"
    def analyze(self, asset_id, asset_symbol, df) -> Signal:
        try: return self._analyze_impl(asset_id, asset_symbol, df)
        except Exception as exc: return Signal(category="technical", score=0.0, confidence=0.0, ...)
    # Zone-mapping: _rsi_to_score(), _macd_to_score(), etc.
    # Weighted average: composite = sum(sub_scores[k] * weights[k] for k in weights)
    # Confidence: agreement ratio * (1.0 - data_quality_penalty)
```

From src/config.py (new weight fields from Plan 01):
```python
weight_fundamental_pe: float = 0.25
weight_fundamental_pb: float = 0.15
weight_fundamental_roe: float = 0.25
weight_fundamental_revenue_growth: float = 0.15
weight_fundamental_dividend_yield: float = 0.10
weight_fundamental_debt_to_equity: float = 0.10
weight_macro_fed_rate: float = 0.25
weight_macro_cpi: float = 0.25
weight_macro_dxy: float = 0.25
weight_macro_usd_idr: float = 0.25
weight_sentiment_fear_greed: float = 0.40
weight_sentiment_reddit: float = 0.60
```

From src/llm/client.py:
```python
async def llm_completion(messages, model=None, fallback_models=None, num_retries=None, timeout=None, response_format=None) -> LLMResult:
LLM_UNAVAILABLE = LLMResult(content="", model_used="none", is_fallback=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four analysis engines (Fundamental, Macro, Sentiment, Event) with tests</name>
  <files>src/engines/fundamental.py, src/engines/macro.py, src/engines/sentiment.py, src/engines/event.py, tests/test_engines/test_fundamental.py, tests/test_engines/test_macro.py, tests/test_engines/test_sentiment.py, tests/test_engines/test_event.py</files>
  <read_first>
    - src/engines/base.py (BaseEngine ABC, Signal dataclass)
    - src/engines/technical.py (zone-mapping pattern, weighted average, confidence calculation, error handling)
    - src/engines/quantitative.py (graceful degradation pattern)
    - src/config.py (engine weight fields)
    - src/data/analyze.py (_failed_signal pattern)
  </read_first>
  <behavior>
    FundamentalEngine:
    - Test 1: analyze() with valid fundamentals dict returns score in [-1, 1], confidence > 0
    - Test 2: analyze() with crypto asset (fundamentals=None) returns score=0, confidence=0, reasoning contains "not applicable for crypto"
    - Test 3: analyze() with partial data (some None ratios) returns lower confidence
    - Test 4: Each zone mapper returns correct scores: PE<8 -> +0.8, PE>40 -> -0.8, ROE>0.20 -> +0.6, etc.
    - Test 5: analyze() with all None fundamentals returns score=0, confidence=0

    MacroEngine:
    - Test 6: analyze() with full macro_data dict returns valid score
    - Test 7: analyze() with macro_data=None returns score=0, confidence=0
    - Test 8: analyze() reasoning mentions IDX-relevant factors for stocks vs global factors for crypto
    - Test 9: Each zone mapper handles edge cases (0.0, negative, very high values)

    SentimentEngine:
    - Test 10: analyze() with Fear & Greed + Reddit data returns valid score
    - Test 11: analyze() with Fear & Greed only (no Reddit) returns score with lowered confidence (per D-12)
    - Test 12: analyze() with sentiment_data=None returns score=0, confidence=0
    - Test 13: Fear & Greed value 10 (Extreme Fear) maps to positive score (contrarian bullish)

    EventEngine:
    - Test 14: analyze() with news events containing "central_bank" category returns non-zero score
    - Test 15: analyze() with no events returns score=0, confidence=0
    - Test 16: analyze() filters events to only recent ones (today)
  </behavior>
  <action>
**src/engines/fundamental.py:**

Create `FundamentalEngine(BaseEngine)`:
- Constructor: `def __init__(self, fundamentals: dict[str, float | None] | None = None)` where fundamentals keys are: `trailing_pe`, `price_to_book`, `return_on_equity`, `revenue_growth`, `dividend_yield`, `debt_to_equity`
- `category` property returns `"fundamental"`
- `supports_stocks` = True, `supports_crypto` = True (per D-03: keep visible to LLM)
- `analyze()` implementation:
  - If `self._fundamentals is None`: return Signal with score=0, confidence=0, reasoning="Fundamentals not applicable for crypto" (per D-03)
  - Zone-mapping functions (module-level, private):
    - `_pe_to_score(pe: float | None) -> float`: None->0.0, <0->-0.8, <8->0.8, <12->0.5, <18->0.2, <25->-0.2, <40->-0.5, else->-0.8
    - `_pb_to_score(pb: float | None) -> float`: None->0.0, <0.5->0.8, <1.0->0.5, <2.0->0.2, <3.5->-0.2, <6.0->-0.5, else->-0.8
    - `_roe_to_score(roe: float | None) -> float`: None->0.0, <0->-0.7, <0.05->-0.3, <0.10->0.0, <0.15->0.3, <0.20->0.6, else->0.8
    - `_revenue_growth_to_score(rg: float | None) -> float`: None->0.0, <-0.1->-0.7, <0->-0.3, <0.05->0.0, <0.15->0.3, <0.30->0.6, else->0.8
    - `_dividend_yield_to_score(dy: float | None) -> float`: None->0.0, <0.01->-0.2, <0.02->0.0, <0.04->0.3, <0.06->0.6, else->0.8
    - `_debt_to_equity_to_score(de: float | None) -> float`: None->0.0, <0.3->0.8, <0.5->0.5, <1.0->0.2, <2.0->-0.2, <3.0->-0.5, else->-0.8
  - Compute sub_scores dict using zone mappers
  - Weighted average using `settings.weight_fundamental_pe`, etc.
  - Confidence: count non-None ratios / 6 total ratios, scaled by agreement ratio
  - Reasoning: list each ratio value and its interpretation
  - Wrap in try/except returning zero Signal on any error

**src/engines/macro.py:**

Create `MacroEngine(BaseEngine)`:
- Constructor: `def __init__(self, macro_data: dict[str, float] | None = None)` where keys are: `fed_funds_rate`, `cpi`, `dxy`, `usd_idr`
- `category` property returns `"macro"`
- `analyze()` implementation:
  - If `self._macro_data is None` or empty: return Signal with score=0, confidence=0, reasoning="No macro data available"
  - Zone-mapping functions:
    - `_fed_rate_to_score(rate: float | None) -> float`: Higher rates = bearish for risk assets. None->0.0, <1.0->0.6 (accommodative), <3.0->0.2, <5.0->-0.2, <7.0->-0.5, else->-0.8
    - `_cpi_to_score(cpi: float | None) -> float`: Higher CPI = bearish (tightening ahead). None->0.0, <2.0->0.5 (low inflation), <3.0->0.2, <5.0->-0.2, <7.0->-0.5, else->-0.8. NOTE: CPI is an index level; compute YoY change by comparing to value 12 months prior if available, or use raw level as proxy.
    - `_dxy_to_score(dxy: float | None) -> float`: Strong dollar = bearish for IDX/crypto. None->0.0, <90->0.5, <100->0.2, <105->-0.2, <110->-0.5, else->-0.8
    - `_usd_idr_to_score(rate: float | None) -> float`: Weak rupiah = bearish for IDX. None->0.0, <14000->0.5, <15000->0.2, <16000->-0.2, <17000->-0.5, else->-0.8
  - Weighted average using `settings.weight_macro_*` fields
  - One global macro score per D-07. Reasoning highlights IDX-relevant factors (rupiah, BI rate proxy) for stocks and global factors (Fed rate, DXY) for crypto. The `asset_symbol` param determines which factors to emphasize in reasoning (check if symbol looks like a stock ticker).
  - Confidence: count available indicators / 4. Full data -> high confidence.
  - Wrap in try/except

**src/engines/sentiment.py:**

Create `SentimentEngine(BaseEngine)`:
- Constructor: `def __init__(self, sentiment_data: object | None = None)` -- accepts the SentimentSnapshot dataclass from sentiment_fetcher (duck-typed to avoid circular import; access `.fear_greed_value`, `.fear_greed_classification`, `.reddit_posts`)
- `category` property returns `"sentiment"`
- `analyze()` implementation:
  - If `self._sentiment_data is None`: return Signal with score=0, confidence=0
  - Fear & Greed scoring (contrarian): value 0-25 (Extreme Fear) -> bullish +0.7, 25-45 (Fear) -> +0.3, 45-55 (Neutral) -> 0.0, 55-75 (Greed) -> -0.3, 75-100 (Extreme Greed) -> -0.7
  - Reddit scoring: if reddit_posts available for relevant subreddit, compute average post score as a sentiment proxy. Positive sentiment -> positive sub-score, negative -> negative sub-score. Simple heuristic: high-score posts mentioning asset with bullish language -> positive.
  - Actually, per D-10 reddit sentiment is analyzed via LLM. The SentimentEngine should check if `reddit_posts` has LLM-analyzed scores (set by analyze_stage after LLM call). For now, implement a simpler version: if reddit_posts exist, use post count and average score as a rough sentiment signal. The LLM Reddit analysis happens in a separate call within analyze_stage (not inside the engine).
  - Weighted average: Fear & Greed * weight_sentiment_fear_greed + Reddit * weight_sentiment_reddit
  - Per D-12: if no Reddit data, use Fear & Greed only with confidence reduced by 40%
  - Wrap in try/except

**src/engines/event.py:**

Create `EventEngine(BaseEngine)`:
- Constructor: `def __init__(self, news_events: list[dict[str, object]] | None = None)` -- accepts list of dicts with keys: `headline`, `category`, `impact_score`, `affected_assets`
- `category` property returns `"event"`
- `analyze()` implementation:
  - If no events or empty list: return Signal with score=0, confidence=0, reasoning="No events detected"
  - Filter events to those with a `category` in ["central_bank", "earnings", "regulation", "halving", "macro"]
  - Filter to events where `affected_assets` includes the current asset (check `asset_symbol` key in affected_assets dict)
  - If no relevant events: score=0, confidence=0
  - If relevant events: aggregate impact_scores (average of non-None impact_scores for relevant events), set that as the engine score. Confidence = min(0.8, 0.2 * number_of_relevant_events).
  - Reasoning: list top 3 relevant headlines with their category
  - Wrap in try/except

All four engines follow the exact same pattern: try/except at top of analyze(), zone-mapping helpers, weighted average, confidence calculation, Signal return.
  </action>
  <verify>
    <automated>cd /Users/herza/rapier/trade-agent && uv run pytest tests/test_engines/test_fundamental.py tests/test_engines/test_macro.py tests/test_engines/test_sentiment.py tests/test_engines/test_event.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/engines/fundamental.py contains `class FundamentalEngine(BaseEngine):`
    - src/engines/fundamental.py contains `_pe_to_score` zone-mapping function
    - src/engines/fundamental.py contains `reasoning="Fundamentals not applicable for crypto"` for None fundamentals case
    - src/engines/macro.py contains `class MacroEngine(BaseEngine):`
    - src/engines/macro.py contains `FRED_SERIES` or equivalent series reference
    - src/engines/sentiment.py contains `class SentimentEngine(BaseEngine):`
    - src/engines/sentiment.py contains Fear & Greed contrarian scoring logic
    - src/engines/event.py contains `class EventEngine(BaseEngine):`
    - src/engines/event.py filters events by category and affected_assets
    - All four test files pass with 0 failures
  </acceptance_criteria>
  <done>Four engines produce valid Signal outputs. FundamentalEngine zone-maps 6 ratios. MacroEngine zone-maps 4 FRED indicators. SentimentEngine combines Fear & Greed + Reddit. EventEngine aggregates relevant news events. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLM news impact scorer with tests</name>
  <files>src/llm/news_analyzer.py, tests/test_llm/test_news_analyzer.py</files>
  <read_first>
    - src/llm/client.py (llm_completion function, LLMResult, LLM_UNAVAILABLE, response_format usage)
    - src/llm/prompts.py (existing prompt builder patterns)
    - src/db/models.py (NewsEvent model -- impact_score, affected_assets, category columns to update)
    - src/data/analyze.py (analyze_stage for context on where this gets called)
  </read_first>
  <behavior>
    - Test 1: score_news_impact() with list of headlines and watchlist returns updated NewsEvent dicts with impact_score, affected_assets, category
    - Test 2: score_news_impact() handles LLM_UNAVAILABLE by leaving impact_score as None
    - Test 3: score_news_impact() caps input at 50 headlines and 200 chars per headline (per research pitfall 6)
    - Test 4: score_news_impact() parses valid JSON from LLM response correctly
    - Test 5: score_news_impact() handles malformed LLM JSON gracefully
  </behavior>
  <action>
Create `src/llm/news_analyzer.py`:

- System prompt constant `NEWS_SCORING_SYSTEM_PROMPT`:
  ```
  You are a financial news analyst. Score each headline's impact on the listed assets.

  Output valid JSON array where each element has:
  - headline_index: int (0-based index matching input)
  - impact_score: float from -1.0 (very bearish) to +1.0 (very bullish), 0.0 if irrelevant
  - affected_assets: dict of asset_symbol -> relevance_score (0.0 to 1.0)
  - category: one of "central_bank", "earnings", "regulation", "halving", "macro", "sector", "company", "market", "other"

  Only include assets from the watchlist that are meaningfully affected.
  ```

- `async def score_news_impact(session: AsyncSession, watchlist_symbols: list[str]) -> None`:
  - Query `NewsEvent` rows from today where `impact_score IS NULL` (unscored)
  - If no unscored headlines, return
  - Cap at 50 headlines. Truncate each headline to 200 chars.
  - Build user message: numbered headline list + watchlist symbols
  - Call `llm_completion(messages=[system, user], response_format={"type": "json_object"}, timeout=settings.timeout_decide_per_call)`
  - If `LLM_UNAVAILABLE`, log warning and return (headlines remain unscored)
  - Parse JSON response. For each scored headline, UPDATE the corresponding NewsEvent row: set `impact_score`, `affected_assets` (JSONB), `category`
  - Catch JSON parse errors: log warning, skip malformed responses
  - Use structlog with `component="news_analyzer"` binding

- Helper `_build_news_scoring_prompt(headlines: list[tuple[int, str, str]], watchlist: list[str]) -> str`:
  - Format headlines as numbered list: `{i}. [{source}] {headline}`
  - Append watchlist: `Watchlist: {", ".join(watchlist)}`
  - Return formatted user message

**Tests:** Mock `llm_completion` to return canned JSON responses. Test parsing, error handling, and DB update calls.
  </action>
  <verify>
    <automated>cd /Users/herza/rapier/trade-agent && uv run pytest tests/test_llm/test_news_analyzer.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/llm/news_analyzer.py contains `async def score_news_impact(session: AsyncSession, watchlist_symbols: list[str])`
    - src/llm/news_analyzer.py contains `NEWS_SCORING_SYSTEM_PROMPT` with the scoring instruction
    - src/llm/news_analyzer.py contains headline cap of 50 and 200 char truncation
    - src/llm/news_analyzer.py calls `llm_completion` with `response_format={"type": "json_object"}`
    - src/llm/news_analyzer.py handles LLM_UNAVAILABLE by returning without updating
    - tests/test_llm/test_news_analyzer.py exits 0
  </acceptance_criteria>
  <done>LLM news analyzer batch-scores headlines via llm_completion with JSON mode, updates NewsEvent rows with impact_score/affected_assets/category. Handles LLM failures and malformed JSON gracefully. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_engines/ tests/test_llm/test_news_analyzer.py -x -v` all green
- All engines importable: `from src.engines.fundamental import FundamentalEngine`
- FundamentalEngine with None fundamentals returns "not applicable for crypto" reasoning
- MacroEngine with None data returns score=0
- LLM news analyzer importable: `from src.llm.news_analyzer import score_news_impact`
</verification>

<success_criteria>
- Four engines subclass BaseEngine and produce valid Signal outputs
- Zone-mapping pattern consistent with TechnicalEngine
- Constructor injection pattern works for store-backed data
- LLM news analyzer scores headlines and updates DB
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-fundamental-macro-sentiment-and-news-engines/08-03-SUMMARY.md`
</output>
